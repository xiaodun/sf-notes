import React, { FC, ReactNode } from "react";
import SelfStyle from "./PBookEdit.less";
export interface IPBookEditProps {}
const PBookEdit: FC<IPBookEditProps> = (props) => {
  return <div>编辑页面</div>;
};
export default PBookEdit;
