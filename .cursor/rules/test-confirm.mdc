---
description: 测出/验收：一次只报一个问题；先确认再改
alwaysApply: true
---

# 测试与验收：一次一个问题，先确认再改

用户说「测试」「验收」「跑测」「功能测试」「对照 checklist」等，或仅要求分析/结论时：

1. **一次只报一个问题**：测出一个问题就立刻停下，向用户说明现象、复现与建议；等用户处理完或明确说「继续测」后，再测下一项。**禁止**一次输出一大串问题清单。
2. **默认只测、只报**：跑分析/单测/真机或桌面点验；当前这一个问题写清通过/失败与复现步骤。
3. **禁止顺手改业务代码**：不因测例失败、白屏、缺依赖、表现不符预期等，自动改产品与规则实现。
4. **发现阻塞只列选项**：就这一个问题给出可选修法与影响；等用户明确「改吧」「修阻塞」或选定方案后，再动业务代码或合入。
5. **测例文件也先说明**：新增或改测试前，在对话写清测什么、为何需要；用户未要补测时不要主动加。
6. **例外（须用户明说）**：用户要求「修到能过测」「顺手修」「改吧」「把剩下的一起列出来」，或任务本身就是修 bug / 实现功能时，按正常开发流程处理。