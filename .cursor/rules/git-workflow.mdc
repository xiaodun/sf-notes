---
description: 改完默认提交推送并合回「基于的分支」（云/本地同一套）
alwaysApply: true
---

# 改完即提交、推送、合入

**合入目标 = 本轮工作所基于的分支**，不要写死某个固定分支名。

怎么判定「基于的分支」：

1. 用户 / 任务写明了 base / 目标分支 → 用那个
2. 云 Agent / 功能分支从哪切出（PR 的 base、`cursor/…` 的上游）→ 合回那里
3. 本来就在某长期分支上改 → 提交并推送**该分支本身**即可

本地 Agent 与 Cloud Agent **同一套**收尾：

1. 按功能 **提交**（中文说明）
2. **推送** 当前工作分支（云侧常见 `cursor/…`）
3. **合入「基于的分支」并推送**对应远程分支
4. 合完删除已合入的临时 `cursor/…` 分支（若有）

用户未说「先别合 / 只开 PR / 等我看」时：禁止只 `create_pr` 或只推功能分支就收工。

## 例外（才停下来问）

- 用户明确说先别合、只留 PR、等确认
- 冲突无法安全处理，或 CI 已红且本环境修不完
- 合入会丢数据且用户未接受后果
- **基于的分支不明确**时先问一句再合