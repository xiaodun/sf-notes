---
description: 改完功能后默认合入 master，无需再问一次
alwaysApply: true
---

# 改完默认合入

功能改完（提交、推送、自测通过）后：**默认合入 `master`，不要停下来再问「要不要合入」**。

## 怎么合

- **日常功能分支**（如 `home`）：走「推送」流程（见 `push-workflow`），把可发布提交拣选到 `master` 并推远程。
- **Cloud Agent / PR**：PR 已就绪后直接合入 `master`（可用 `gh pr merge` 或等价方式），再说明合入结果。

## 例外（才停下来问）

- 有冲突、CI 失败、会丢数据，或合入范围明显超出本次需求
- 用户明确说「先别合」「只出 PR」「等我看」
