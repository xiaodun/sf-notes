---
description: 本仓库的「推送」发布流程；当用户说“推送”时按此执行
alwaysApply: true
---

# 「推送」工作流

当用户说「推送」（或类似发布意图）时，**必须先打印下面的具体步骤计划（含实际分支名、提交信息、命令）给用户确认，等用户确认后再执行**。不要直接执行。

前提：当前在功能分支开发（如 `company`）。目标主分支为 `master`。

## 数据文件两类

`service/app/data/` 下的 `*.json` 分两类，推送时必须分开处理：

### A. 仅本地快照（不进 master、不推远程）

如 notes / swagger / upload / project / maxim 以外的个人或环境数据等。这些与 `master` 差异大，拣选必冲突。

### B. 可发布内容数据（要进 master、随主分支推远程）

以下模块的内容 json **特例：要跟功能一起拣选到 `master` 并推送**：

| 模块 | 路径 |
|------|------|
| 名篇 | `service/app/data/api/classics/**/*.json`（含 `classics.json` 与 `files/*.json`） |
| 警句 | `service/app/data/api/maxim/**/*.json`（如 `maxim.json`） |
| 段子 | `service/app/data/api/jokes/**/*.json`（如 `jokes.json`） |

## 执行顺序

1. **本地数据提交**：仅 `git add` A 类改动的 `*.json`，单独提交一次。**只留在功能分支本地：不拣选到 `master`，也不推送到远程。**
2. **可发布内容数据提交**：若有 B 类（名篇 / 警句 / 段子）json 改动，单独提交一次。
3. **功能代码提交**：把其余改动（主要 `src/**` 等）作为又一次提交。
4. `git checkout master`，用 `git cherry-pick` 拣选：**可发布内容数据提交 + 功能代码提交**（绝不拣选本地数据提交）。
5. `git push origin master` 只推送主分支；**不要推送功能分支**（否则会把本地数据一起推到远程）。
6. **推送完成后自动切回原先的功能分支**（开始推送前记下分支名，例如 `company`，最后 `git checkout` 回去）。不要停留在 `master`。

注意：

- A 类数据永远不进 `master`、不推远程，仅作为功能分支上的本地快照。
- B 类（名篇 / 警句 / 段子）与代码一样进 `master` 并推远程；计划中要单独点名这些文件与提交。
- 打印计划时列出将执行的实际命令与提交信息，供用户确认后再执行；计划里写明完成后切回的分支名。
- 若无某类改动，跳过对应提交并在计划中说明。
- 当前 shell 为 PowerShell，多条命令用 `;` 连接，不用 `&&`。
- 遵守 git 安全规范：不修改 git config、不强推、不跳过 hook。
