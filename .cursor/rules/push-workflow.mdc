---
description: 本仓库的「推送」发布流程；当用户说“推送”时按此执行
alwaysApply: true
---

# 「推送」工作流

当用户说「推送」（或类似发布意图）时，**必须先打印下面的具体步骤计划（含实际分支名、提交信息、命令）给用户确认，等用户确认后再执行**。不要直接执行。

前提：当前在功能分支开发（如 `company`）。目标主分支为 `master`。

执行顺序：

1. 数据提交：仅 `git add service/app/data/` 下改动的 `*.json`（如 notes / swagger / upload），单独提交一次。**该数据提交只留在功能分支本地：不拣选到 `master`，也不推送到远程。**
2. 功能代码提交：把其余改动（主要 `src/**` 等）作为第二次提交。
3. `git checkout master`，用 `git cherry-pick` **只拣选功能代码提交**到 `master`（绝不拣选数据提交）。
4. `git push origin master` 只推送主分支；**不要推送功能分支**（否则会把本地数据一起推到远程）。
5. 完成后停留在 `master`。

注意：

- 数据 `json` 与 `master` 上的版本差异很大，拣选必然冲突，所以数据永远不进 `master`、不推远程，仅作为功能分支上的本地快照。
- 打印计划时列出将执行的实际命令与提交信息，供用户确认后再执行。
- 若无数据文件改动或无功能代码改动，跳过对应提交并在计划中说明。
- 当前 shell 为 PowerShell，多条命令用 `;` 连接，不用 `&&`。
- 遵守 git 安全规范：不修改 git config、不强推、不跳过 hook。
