---
description: 表单输入框默认要支持一键清空
alwaysApply: true
---

# 输入框支持清空

新增或改动页面里的文本输入（Ant Design `Input` / `Input.TextArea`、受控输入框等）时，**默认加上清空能力**：

- Ant Design：用 `allowClear`
- 其它自定义输入：提供等价的清空交互

例外（才可不加）：

- 密码框等明确不适合一键清空的场景
- 用户明确说不要清空

不要只在某一个页面做特例；同类表单输入都应可清空。
